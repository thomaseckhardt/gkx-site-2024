import Splide from '@splidejs/splide'

export function projectThumbnails() {
  return {
    init() {
      // const thumbnails = this.$refs.thumbnails
      // console.log('projectThumbnails', { thumbnails })
      // if (thumbnails) {
      //   const splide = new Splide(thumbnails, {
      //     type: 'loop',
      //     fixedWidth: 'var(--nav-item-w)',
      //     gap: '0.125rem',
      //     pagination: false,
      //     focus: 'center',
      //   }).mount()
      //   console.log('projectThumbnails.mounted', { splide })
      // }
    },
  }
}
